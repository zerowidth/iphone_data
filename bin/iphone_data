#!/usr/bin/env ruby

require File.expand_path(
    File.join(File.dirname(__FILE__), '..', 'lib', 'iphone_sms'))

require "yaml"
IPhoneSMS::IPhoneData.iphones.first.sms_messages.each { |m| puts m.to_s(:log) }
